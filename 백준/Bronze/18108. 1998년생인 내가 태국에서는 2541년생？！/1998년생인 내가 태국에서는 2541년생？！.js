const input = require('fs').readFileSync('/dev/stdin').toString().split(' ').map(Number);
const currentYear = 2025;
const thYear = 2562;
const plus = thYear - currentYear;
console.log(input - 543)
